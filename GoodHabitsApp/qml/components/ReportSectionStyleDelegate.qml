import Felgo 3.0
import QtQuick 2.0

/// Style definition for graphical element section for Report page.
StyleSimpleSection {
    backgroundColor: Theme.colors.disabledColor
    textHorizontalAlignment: Qt.AlignHCenter
    textVerticalAlignment: Qt.AlignVCenter
    showDividers: true
    fontBold: true
    textColor: Theme.colors.textColor
    totalHeight: dp(20)
}
